# DevOps Incident Tracker

A FastAPI backend for ingesting, storing, and filtering application events.

## Current Features

- Create events through a REST API
- Retrieve and filter events
- Validate incoming event data
- Persist events using SQLAlchemy and SQLite